{
  "name": "webhook-proxy",
  "version": "1.0.0",
  "description": "Secure Vercel Webhook Proxy",
  "main": "api/send.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.8" 
  }
}
